import{orthogonalNeighbors,whiteCells}from'./model.js';
import{lightSourcesFor,visibleFrom,litCells}from'./rules.js';
export function nextHumanStep(puzzle,{bulbs=new Set(),noBulbs=new Set()}={}){for(const bulb of bulbs)for(const cell of visibleFrom(bulb,puzzle))if(!bulbs.has(cell)&&!noBulbs.has(cell))return{kind:'visibility',index:cell,value:0,reason:'To pole jest już oświetlane przez latarnię, więc nie może zawierać kolejnej latarni.',area:[bulb,cell]};for(const wall of puzzle.walls||[]){if(!Number.isInteger(wall.clue))continue;const cells=orthogonalNeighbors(wall.index,puzzle).filter(i=>!(puzzle.walls||[]).some(w=>w.index===i));const placed=cells.filter(i=>bulbs.has(i));const available=cells.filter(i=>!bulbs.has(i)&&!noBulbs.has(i));if(placed.length===wall.clue&&available.length)return{kind:'clue-complete',index:available[0],value:0,reason:`Ściana z liczbą ${wall.clue} ma już wymaganą liczbę latarni.`,area:[wall.index,...cells]};if(placed.length+available.length===wall.clue&&available.length)return{kind:'clue-forced',index:available[0],value:1,reason:`Przy ścianie z liczbą ${wall.clue} wszystkie pozostałe dostępne pola muszą zawierać latarnie.`,area:[wall.index,...cells]};}
const lit=litCells(puzzle,bulbs);for(const cell of whiteCells(puzzle)){if(lit.has(cell))continue;const possible=lightSourcesFor(cell,puzzle).filter(source=>!noBulbs.has(source));if(possible.length===1)return{kind:'only-light-source',index:possible[0],value:1,reason:'To pole może zostać oświetlone tylko z jednego miejsca, więc właśnie tam musi stanąć latarnia.',area:[cell,possible[0]]};}return null;}
export function humanSolve(puzzle){const bulbs=new Set(),noBulbs=new Set(),steps=[];const scoring={visibility:1,'clue-complete':1,'clue-forced':2,'only-light-source':4};for(let i=0;i<puzzle.rows*puzzle.cols*8;i++){const step=nextHumanStep(puzzle,{bulbs,noBulbs});if(!step)break;steps.push(step);if(step.value===1)bulbs.add(step.index);else noBulbs.add(step.index);}return{bulbs,noBulbs,steps,score:steps.reduce((s,x)=>s+(scoring[x.kind]||1),0)};}
